# Mount the dsh-scope plugin when this package is registered as a
# DeepSeek Harness profile bundle. The host half is an empty shell (pure UI
# plugin); this entry only puts it on the Loader's radar.
- insert:
    - id: dsh-scope
      name: dsh-scope
